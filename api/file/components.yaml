components:
  schemas:
    UploadedFile:
      type: object
      properties:
        content:
          type: string
          description: Base64URL로 인코딩 된 파일 내용
        ext:
          type: string
          description: 파일 확장자
        name:
          type: string
          description: 파일 이름
    LoadedFile:
      type: object
      properties:
        content:
          type: string
          description: Base64URL로 인코딩 된 파일 내용
        ext:
          type: string
          description: 파일 확장자
        name:
          type: string
          description: 파일 이름
    RunResult:
      type: object
      properties:
        result:
          type: string
          description: 실행 결과
