components:
  schemas:
    AuthRequest:
      type: object
      properties:
        email:
          type: string
          description: 이메일
        password:
          type: string
          description: 비밀번호
    AuthToken:
      type: object
      properties:
        access-token:
          type: string
          description: 인증 토큰
